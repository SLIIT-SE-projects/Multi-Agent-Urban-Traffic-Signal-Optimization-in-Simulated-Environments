# Simulation Manager — Python dependencies
#
# Phase 2: keeps in-process adapters (GNN/MPC/EVPS) so all heavyweight ML
# libraries are still required here. Phases 3-5 split each model into its
# own service; the corresponding deps will move out then.

# ── Web framework ────────────────────────────────────────────────────────
flask==3.0.0
flask-cors==4.0.0
flask-socketio==5.3.6
flask-sock==0.7.0
eventlet==0.33.3
python-dotenv==1.0.0

# ── HTTP client (Phase 3+) — used by HttpModelRouter to call model services ─
requests>=2.32.0

# ── SUMO Python integration ──────────────────────────────────────────────
# Both packages are pip-installable WITHOUT needing the SUMO C++ binary.
# In SUMO_MODE=remote the binary lives on the host.
traci>=1.18.0
sumolib>=1.18.0

# ── In-process adapter dependencies (Phase 2 only) ───────────────────────
# Phase 3: torch + torch-geometric move to gnn_service/
# Phase 4: numpy + pandas stay (used by simulation_controller helpers too)
# Phase 5: tensorflow + scikit-learn move to evps_service/
torch>=2.0.0
torch-geometric>=2.4.0
numpy>=1.24.0
pandas>=2.0.0
tensorflow>=2.15.0
scikit-learn>=1.3.0
